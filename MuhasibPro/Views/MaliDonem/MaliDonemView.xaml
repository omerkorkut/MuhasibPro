<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="MuhasibPro.Views.MaliDonem.MaliDonemView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:MuhasibPro.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:MuhasibPro.Views.MaliDonem"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:MuhasibPro.Views.MaliDonem"
    mc:Ignorable="d">

    <Grid>
        <controls:WindowTitle Title="{x:Bind ViewModel.Title, Mode=OneWay}" />
        <controls:Section IsEnabled="{x:Bind ViewModel.IsEnabled, Mode=OneWay}" Style="{StaticResource DetailsViewSection}">
            <views:MaliDonemDetails x:Name="details" ViewModel="{x:Bind ViewModel, Mode=OneWay}" />
        </controls:Section>
    </Grid>
</Page>