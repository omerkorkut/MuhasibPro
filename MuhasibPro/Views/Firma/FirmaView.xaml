<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MuhasibPro.Views.Firma.FirmaView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:MuhasibPro.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:MuhasibPro.Views.Firma"
    mc:Ignorable="d">

    <Grid>
        <controls:WindowTitle Title="{x:Bind ViewModel.FirmaDetails.Title, Mode=OneWay}" />
        <controls:Section            
            IsEnabled="{x:Bind ViewModel.FirmaDetails.IsEnabled, Mode=OneWay}"
            Style="{StaticResource DetailsViewSection}">
            <views:FirmaDetails x:Name="details" ViewModel="{x:Bind ViewModel, Mode=OneWay}" />
        </controls:Section>
    </Grid>
</Page>